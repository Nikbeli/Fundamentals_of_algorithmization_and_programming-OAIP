#pragma once
#include<assert.h>
#include <ctype.h>


int my_strlen1(const char* p);

int my_strlen2(const char* p);

int strcmp(const char* strX, const char* strY);

char* strcpy(char* destination, const char* source);

char* strcat(char* str1, char* str2);